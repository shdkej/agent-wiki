---
title: "README"
type: mapped-note
tags: [mapped, source-boundary, knowledge-lab, mandalart]
source_file: "[[source/shdkej-content/README.md]]"
source_name: README
created: 2026-05-18
updated: 2026-05-20 01:17 UTC
status: developed
language: ko
---

# README

- 원본: `source/shdkej-content/README.md`
- 상태: 점진 심화됨
- 역할: 원본 만다라트 레이어 자체의 경계와 취급 방식을 설명하는 루트 카테고리 페이지

## 한눈에 보는 요약
이 노드는 사용자의 원본 노트 내용을 직접 요약하는 페이지라기보다, `source/shdkej-content/` 전체를 어떻게 다뤄야 하는지 알려 주는 경계 표식이다. 핵심은 원본 레이어를 작업 재료로 마음대로 고치는 공간이 아니라, 주제 경계와 폴더 구조를 보존한 채 `agent-wiki/`에서 해석·비교·합성을 수행해야 하는 1차 소스로 보는 것이다.

## 핵심 논지
`source/shdkej-content/`는 임의 문서 저장소가 아니라 사용자의 원본 content/노트에서 온 1차 소스 레이어다. 따라서 이 레이어의 가치는 개별 문장뿐 아니라 폴더 구조, 파일 단위, 주제 경계, 수정 절차에 함께 있다. 좋은 에이전트 정리는 원본을 깨끗하게 덮어쓰는 일이 아니라, 원본은 그대로 두고 대응 위키 페이지에서 해석 가능한 구조를 만드는 일이다.

이 README는 다른 카테고리처럼 특정 주제를 길게 설명하지 않는다. 대신 전체 만다라트 원본을 다루는 `출입 규칙`에 가깝다. 이 노드가 약하면 각 mapped 노트는 내용 요약에는 성공해도, 원본의 거친 순서·비어 있는 항목·중복 질문을 왜 보존해야 하는지 잊기 쉽다.

## 왜 이 노드가 중요한가
- 1:1 mapped 원칙의 기준점이다. 다른 노트들이 각 카테고리 내용을 담는다면, 이 노드는 그 카테고리들을 어떻게 취급할지 정한다.
- [[Portfolio]], [[CHANGELOG]], [[Meta/Collection]] 같은 루트·메타 노드를 읽을 때, 원본의 불완전함이나 거친 기록을 함부로 삭제하지 말아야 하는 이유를 제공한다.
- `source/external-links/`와 달리 이 레이어가 사용자의 원본 만다라트 구조에서 온 1차 소스임을 구분하게 만든다.
- 이후 drift 점검, human-review 복사, source 수정 금지 같은 운영 절차의 근거가 된다.

## 원본에서 직접 보이는 구조

### 1. 레이어 선언
원문 첫 문장은 `source/shdkej-content/`가 사용자의 원본 content/노트에서 온 1차 소스라고 못박는다. 여기서 중요한 단어는 `원본`과 `1차 소스`다. 에이전트가 이 폴더를 정리 대상 폴더로 보면 안 되고, 위키가 참조해야 할 출처 레이어로 봐야 한다.

### 2. 읽기 전용 원칙
규칙의 첫 항목은 본문을 기본적으로 읽기 전용으로 둔다고 말한다. 이것은 단순한 파일 보호가 아니라 해석 책임의 분리다.

- 원본에는 사용자의 실제 기록 습관, 빈칸, 중복, 덜 익은 질문이 남아 있다.
- agent-wiki에는 그 재료를 읽어낸 구조, 요약, 연결, 판단이 남는다.
- 원본이 불편해 보여도 에이전트가 먼저 손대면, 나중에 선호 변화와 실제 기록 흔적을 구분하기 어려워진다.

### 3. 폴더 구조와 주제 경계 보존
원문은 폴더 구조와 주제 경계를 유지하라고 한다. 이는 이 랩에서 각 markdown 노트를 8x8 만다라트식 카테고리 노드로 보는 규칙과 바로 연결된다.

즉 파일은 단순한 글 조각이 아니라 다음 정보를 함께 가진다.

| 요소 | 읽는 법 |
| --- | --- |
| 폴더 위치 | 이 노트가 어느 생활·기술·사고 영역에 놓이는지 보여 준다. |
| 파일 단위 | 사용자가 하나의 카테고리로 묶어 본 사고 단위다. |
| 빈칸과 미완성 | 삭제 대상이 아니라 아직 질문이 덜 자란 상태 신호일 수 있다. |
| 중복과 거친 표현 | 정리 실패가 아니라 어떤 질문이 반복해서 돌아오는지 보여 주는 흔적이다. |

## 만다라트 구조에서의 위치

루트 README는 만다라트의 한 칸이라기보다, 격자 전체를 어떻게 만질지 정하는 표면이다. 그래서 이 노드를 읽을 때는 `무슨 내용이 들어 있는가`보다 `내용을 어디에서 바꿀 수 있는가`를 먼저 봐야 한다.

- `source/shdkej-content/`의 각 markdown 노트는 원본 카테고리 노드다.
- `agent-wiki/content/docs/mapped/`의 각 대응 문서는 그 노드를 해석한 컴파일 결과다.
- 루트 README는 두 레이어가 서로 섞이지 않게 하는 경계 문서다.
- 이 경계가 유지되어야 이후 external-links, syntheses, concepts가 원본 만다라트의 의미를 덮어쓰지 않는다.

## 운영 해석

### source와 agent-wiki는 역할이 다르다
원문은 정리, 해석, 비교, 합성을 `agent-wiki/`에서 수행하라고 한다. 따라서 mapped 노트는 원본을 예쁘게 복사한 두 번째 원본이 아니라, 원본을 다시 사용할 수 있게 만드는 컴파일 레이어다.

- source: 원래 기록의 위치, 질감, 불완전함을 보존한다.
- mapped: 원본과 1:1로 대응하며 핵심 구조를 읽어 낸다.
- syntheses/concepts: 여러 mapped 노드에서 반복되는 판단을 장기 지식으로 올린다.
- log: 어떤 노드를 언제 어떤 관점으로 갱신했는지 남긴다.

### 원본 수정은 별도 사건이다
원문은 원본 수정이 필요하면 변경 이유와 영향을 분리해서 기록하라고 한다. 이것은 사용자의 직접 수정이 들어왔을 때 `human-reviews/`에 복사하고 선호 차이를 문서화하라는 운영 규칙과 같은 방향이다.

중요한 점은 원본과 에이전트 해석을 섞지 않는 것이다. 사용자가 원본을 고쳤다면 그것은 단순 최신화가 아니라 선호, 관심사, 구조 감각이 바뀐 증거일 수 있다. 에이전트는 그 차이를 평탄화하지 말고, 무엇이 추가·삭제·강조되었는지 따로 읽어야 한다.

## 편집 판단으로 번역하기

이 README가 실제 hourly run에 주는 판단은 단순하다.

- 원본이 짧아도 대응 mapped 노트는 `왜 짧은지`, `어떤 경계를 세우는지`, `다른 노트와 어떤 역할 차이가 있는지`를 설명할 수 있다.
- 원본에 직접 손대고 싶은 충동이 생기면, 먼저 mapped 노트나 log에 해석을 남기는 것이 기본 경로다.
- 사용자가 source를 직접 고친 흔적이 있으면 최신 내용을 덮어쓰기보다 변화 전후의 선호 차이를 별도로 보존한다.
- 외부 링크가 많은 날에도 현재 선택한 source 노트의 주제 경계가 우선이며, 외부 자료는 보조 렌즈로만 쓴다.

### 우선순위는 풍부함이 아니라 출처 경계에서 나온다
이 README가 특히 막아야 하는 드리프트는 `자료가 더 많아 보이는 쪽`으로 작업 중심이 이동하는 것이다. `source/external-links/`는 빠르게 늘어나고 주제가 선명한 자료도 많지만, 이 README의 기준에서 원본 만다라트 노드를 대체하는 1차 소스가 아니다. 따라서 hourly run에서 어떤 노트를 고를지 판단할 때는 외부 자료의 양보다 `source/shdkej-content/`의 1:1 대응 상태, mapped 페이지의 성숙도, 최근 사용자 수정 여부를 먼저 봐야 한다.

이 기준은 단순한 보수성이 아니다. 원본 노트가 짧고 외부 링크가 길어도, 현재 작업 대상이 원본 README라면 핵심 질문은 여전히 `원본 레이어를 어떻게 다룰 것인가`다. 외부 링크는 이 질문을 설명하는 보조 사례가 될 수는 있지만, README를 외부 자료 수집 규칙 전체의 안내서로 바꾸면 루트 경계 문서의 역할이 흐려진다.

## 레이어별 책임 분리

원문 마지막 규칙은 자세한 source 규칙을 `schema/source.md`에 위임한다. 이 위임은 세부 규칙을 다른 파일에 숨기는 것이 아니라, README가 너무 많은 운영 절차를 직접 떠안지 않도록 경계를 나누는 장치다. 따라서 이 노드는 아래처럼 원본 취급과 에이전트 해석의 경계선을 계속 판정하는 기준점으로 읽어야 한다.

| 레이어 | 맡는 책임 | README가 주는 금지선 |
| --- | --- | --- |
| `source/shdkej-content/` | 사용자의 원본 기록, 폴더 구조, 주제 경계 보존 | 보기 좋게 만들기 위해 본문을 임의 수정하지 않는다. |
| `agent-wiki/content/docs/mapped/` | 각 source 노트의 1:1 해석, 구조화, 연결 | 여러 노트를 합쳐 원본 격자를 흐리지 않는다. |
| `agent-wiki/content/docs/concepts/`·`syntheses/` | 여러 노드에서 반복되는 판단과 패턴 승격 | 단일 mapped 노트 안에 과도한 일반론을 밀어 넣지 않는다. |
| `human-reviews/` | 사용자가 source를 직접 고친 뒤의 변화 비교 | 최신 원문을 에이전트 해석으로 평탄화하지 않는다. |
| `agent-wiki/log.md` | 어떤 노드를 어떤 관점으로 바꿨는지 남기는 작업 경로 | 작은 편집을 흩어진 로그로 남발하지 않고 phase 단위로 묶는다. |

이 표가 중요한 이유는 원본 README가 매우 짧기 때문이다. 짧은 규칙 문서는 쉽게 `내용이 부족한 노트`로 오해되지만, 실제로는 나머지 76개 카테고리 노트를 건드릴 때마다 호출되는 상위 계약이다. 그래서 이 mapped 페이지의 확장은 원문에 없는 일반론을 덧붙이는 일이 아니라, 원문 다섯 줄 규칙이 매시간 편집 판단에서 어떻게 작동해야 하는지 풀어 쓰는 일에 가깝다.

## 짧은 경계 문서를 다루는 법

이 README처럼 원문이 짧은 문서는 `분량 대비 해석이 과한가`를 항상 의식해야 한다. 다만 짧다는 사실만으로 mapped도 짧아야 하는 것은 아니다. 경계 문서는 본문 정보량보다 적용 범위가 넓기 때문에, 좋은 mapped 정리는 아래 세 가지를 보강할 수 있다.

- **적용 범위**: 어떤 파일과 레이어에 영향을 주는 규칙인지 분명히 한다.
- **오해 방지**: 원본 수정, 외부 링크 흡수, 여러 노드 병합처럼 자주 생기는 드리프트를 미리 막는다.
- **실행 기준**: hourly run에서 어떤 순서로 읽고, 어디에 쓰고, 무엇을 기록할지 바로 판단할 수 있게 한다.

반대로 이 노드에 넣지 말아야 할 것은 각 카테고리의 실제 내용 해석이다. 예를 들어 Portfolio의 운영 성과, CHANGELOG의 유지 박동, Meta/Collection의 수집 기준은 이 README와 연결될 수는 있지만, 그 자체의 분석은 각 mapped 노트에 남아야 한다. README는 내용을 대신 설명하는 문서가 아니라, 그 내용들이 자기 자리를 잃지 않게 하는 경계 문서다.

## 드리프트를 막는 판별 기준

이 루트 README를 실제 작업 중에 다시 호출해야 하는 순간은 대체로 노트가 너무 잘 정리되고 싶어질 때다. 원본을 더 매끄러운 지식 문서로 바꾸고 싶은 욕구가 생기면, 먼저 아래 기준으로 어떤 레이어에서 처리해야 하는지 나눠야 한다.

| 상황 | 처리 위치 | 이유 |
| --- | --- | --- |
| 원문 표현이 거칠거나 중복된다 | mapped 노트에서 해석한다 | 거친 표현은 사용자의 실제 사고 흔적일 수 있다. |
| source 폴더 구조가 비효율적으로 보인다 | log나 synthesis에서 관찰한다 | 구조 변경은 만다라트 위치 의미를 바꿀 수 있다. |
| 외부 링크가 원본 주제보다 더 풍부하다 | external-links 보조 맥락으로 제한한다 | 외부 자료가 원본 카테고리를 대체하면 1:1 대응이 흐려진다. |
| 사용자가 원본을 직접 고쳤다 | human-reviews에 비교본을 남긴다 | 최신화보다 선호 변화와 강조점 이동을 먼저 읽어야 한다. |
| 여러 mapped 노드에서 같은 패턴이 반복된다 | concepts나 syntheses로 올린다 | 단일 mapped 노트가 과도하게 종합 문서가 되는 것을 막는다. |

따라서 이 README의 핵심 기능은 `source를 보존하라`는 금지 규칙 하나가 아니다. 더 정확히는 **어떤 판단을 어느 레이어에 남길지 정하는 라우터**다. source는 원본 흔적을 보존하고, mapped는 대응 노드의 해석을 맡고, synthesis는 반복 패턴을 올리며, log는 작업 경로를 남긴다. 이 분업이 유지될 때 Knowledge Lab은 깨끗한 요약집이 아니라 사용자의 사고 격자를 잃지 않는 장기 위키가 된다.

## 다른 노드와의 연결
- [[Portfolio]]: 원본 내용이 실제 실행·운영 성과를 담는 루트 정체성 노드라면, 이 README는 그 원본을 다루는 절차적 정체성 노드다.
- [[CHANGELOG]]: 공개 표면의 유지 박동을 보여 주는 기록이다. 이 README는 그런 거친 운영 기록도 원본 흔적으로 보존해야 함을 정한다.
- [[Meta/Collection]]: 작은 미해결 질문을 탐색 큐로 남기는 노드다. 이 README의 source 보존 원칙이 있어야 Collection의 미완성 질문도 삭제되지 않는다.
- [[Communication/Document]]: 문서가 어떤 재진입 표면을 맡는지 다룬다. 이 README는 source 레이어의 재진입 표면 자체를 정의한다.

## 에이전트가 다음에 지켜야 할 체크포인트
- 대응 source 노트를 먼저 전체 읽고, 기존 mapped 노트를 전체 읽은 뒤에만 편집한다.
- 한 번의 hourly run에서는 mapped 노트 하나만 수정한다.
- 원본 파일을 더 깔끔하게 만들고 싶어도 source 본문은 기본적으로 건드리지 않는다.
- 외부 링크 자료는 현재 선택한 원본 노트의 주제 경계 안에서만 보조 맥락으로 쓴다.
- 원본이 직접 수정된 흔적이 있으면 `human-reviews/`에 비교본을 남기고 선호 변화로 읽는다.

## 현재 상태
이번 baseline은 기존 대응 mapped 페이지가 없던 상태에서, 원문 11줄의 역할을 `source 레이어 경계 노드`로 복원한 것이다. 2026-05-18 07:17 UTC 점진 심화에서는 이 노드를 루트 경계 문서로 더 분명히 세우고, 만다라트 구조 안에서 `카테고리 내용`보다 `편집 권한과 해석 위치`를 정하는 문서라는 점을 보강했다. 2026-05-19 07:17 UTC 점진 심화에서는 원본 보존 원칙을 실제 편집 중의 레이어 라우팅 기준으로 더 선명하게 압축했다. 2026-05-19 18:17 UTC 점진 심화에서는 짧은 경계 문서가 어떻게 source, mapped, synthesis, human-review, log의 책임 분리를 계속 판정하는지 보강했다. 2026-05-20 01:17 UTC 점진 심화에서는 외부 링크나 보조 자료의 풍부함이 원본 만다라트 노드의 우선순위를 빼앗지 않도록, 작업 선택 기준을 `1:1 대응 상태`, `mapped 성숙도`, `사용자 수정 여부` 쪽으로 다시 고정했다. 내용 확장은 원문 밖의 새 주제를 끌어오기보다, 이미 Knowledge Lab이 쓰고 있는 1:1 mapping, 읽기 전용 source, agent-wiki compilation, human-review drift 절차를 이 README의 의미 안에 정렬하는 데 집중했다.
